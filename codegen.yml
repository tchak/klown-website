overwrite: true
schema: ${API_URL}
documents: "app/queries/**/*.graphql"
generates:
  app/graphql-operations.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
